# views tests

VIEW execution against a live MySQL server: view creation, querying and dropping, plus materialized-view style execution coverage.

## Key files

- `test_view_execution.py` — VIEW and materialized view execution
