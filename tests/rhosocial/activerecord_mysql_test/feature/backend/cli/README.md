# cli tests

Tests for the MySQL backend CLI subpackage: argument parsing, help output, serialization, provider factory, protocol-info building, named-expression / introspection / query / procedure argument handling and display functions.

## Key files

- `test_cli.py` — CLI parse/help/serialize/provider/display coverage
