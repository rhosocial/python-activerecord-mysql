Added production-style MySQL time partition operation tests covering microsecond `DATETIME(6)` boundaries, future partition pre-creation, continuous partition pruning, index usage, and cold archival via `EXCHANGE PARTITION`.