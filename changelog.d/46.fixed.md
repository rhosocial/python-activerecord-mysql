fix(tests): use DataType instances instead of raw strings in niladic DDL tests

ColumnDefinition now validates that data_type must be a DataType
instance. Update test_niladic_functions.py to use IntegerType(),
TimestampType() etc. instead of raw string type names.
