Enabled `cache: 'pip'` on all `actions/setup-python@v6` steps in the CI workflow to reuse the pip download cache across runs, while retaining the explicit `pip install -r ...` steps so dependencies still install to the latest specified version. Switched the experimental `test-experimental-python-315` job to `ubuntu-26.04` to use the now-available `3.15.0-beta.4` Linux packages published in `actions/python-versions`.
